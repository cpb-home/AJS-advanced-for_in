import orderByProps, { obj, sorting } from '../app';

console.log(obj, sorting);
orderByProps(obj, sorting);

/*test('should sum', () => {
  const result = sum([1, 2, 3]);

  expect(result).toBe(6);
});*/
